# ==========================================
# Ghostty Configuration
# ==========================================

# Font
font-family = Noto Sans Mono CJK JP
font-size = 18

# Theme - Using built-in theme instead of rose-pine
theme = catppuccin-macchiato
background-opacity = 0.70

# Window
window-decoration = false
window-vsync = true
window-padding-x = 4
window-padding-y = 4
window-padding-balance = true
window-padding-color = extend-always
window-theme = dark

# Mouse
copy-on-select = false

# Selection & Clipboard
# copy-on-select is already set above

# Shell Integration - disabled for faster startup
shell-integration = none

# Scrollback
scrollback-limit = 10000

# Cursor
cursor-style = bar
cursor-style-blink = false

# Keybindings
confirm-close-surface = false

# Environment
env = TERM=xterm-256color
env = LANG=ja_JP.UTF-8
env = LC_ALL=ja_JP.UTF-8

# Performance optimizations
gpu-acceleration = true
gtk-single-instance = true  # Changed to true for faster startup
confirm-close-surface = false  # Skip confirmation dialog
resize-increment = 1  # Faster resize
