// Zellij configuration - Minimal for mobile SSH

keybinds clear-defaults=true {
    shared_except "normal" "locked" {
        ctrl+p { SwitchToMode "Session"; }
    }
    session {
        ctrl+d { Detach; }
    }
}
