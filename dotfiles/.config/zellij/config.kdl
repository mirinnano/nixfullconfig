// Zellij configuration for mobile SSH
// Based on https://zellij.dev/documentation/keybindings.html

keybinds clear-defaults=true {
    shared_except "locked" {
        bind "Ctrl p" { SwitchToMode "Session"; }
    }
    session {
        bind "Ctrl d" { Detach; }
    }
}
