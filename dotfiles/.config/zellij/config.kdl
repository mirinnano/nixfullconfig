// Zellij configuration - Simplified for mobile SSH

keybinds clear-defaults=true {
    shared_except "lock" {
        ctrl+p { SwitchToMode "Session"; }
    }
    session {
        ctrl+d { Detach; }
    }
}
